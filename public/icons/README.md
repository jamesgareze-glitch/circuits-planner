These are solid-color placeholder icons generated by `scripts/generate-icons.mjs`.
Swap `icon-180.png`, `icon-192.png`, and `icon-512.png` for real artwork whenever
you like — same filenames, square PNGs, no other changes needed.
