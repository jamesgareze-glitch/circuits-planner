App icon (person lifting dumbbells), recolored to match the app's theme
green (#16a34a). Source: a VectorStock icon, cropped to remove the preview
watermark. Same filenames as before — `icon-180.png`, `icon-192.png`,
`icon-512.png` — so swap them for different artwork later with no other
changes needed.
